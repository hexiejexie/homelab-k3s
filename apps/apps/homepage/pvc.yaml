apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-config
  namespace: apps
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 256Mi